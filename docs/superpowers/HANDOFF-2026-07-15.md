# Overnight handoff — 2026-07-15 (READ THIS FIRST, then continue)

Owner is asleep. Two jobs, in order. This file is the source of truth — do NOT rely on chat memory; verify state against git + the files named here. Update this file after every milestone so a fresh session can resume precisely.

## Guardrails (binding)
- Revert safety: tag `router-v1-clean` (@ 8d54da9) = clean pre-conviction-trim state. Conviction trim work is on branch `conviction-trim`.
- Conviction trim ships **default-off** (`conviction_trim: bool = False`), byte-identical when off. Merging it dormant is fine; turning it ON in practice is the OWNER's call after reading the A/B.
- **Job 2 (audit) is READ-ONLY.** Find + report everything. Do NOT patch the merged engine overnight. Any real bug goes to the top of the morning report for the owner to direct the fix.
- Standing rules: ELI10 every spec/verdict in chat ([[eli10-specs]]); unseen tickers (XBI EEM EWZ TLT ARKK QQQ) never touched — runner guards are allow-lists (SEEN only); every result labeled in-sample until the wheel basket run releases fresh data.

## JOB 1 — finish conviction trim (spec 2026-07-15-conviction-trim-design.md, plan 2026-07-15-conviction-trim.md)

State at handoff: engine + tests WRITTEN and PASSING (24 router tests green incl. trim), but the commit was interrupted — **working tree may hold uncommitted changes** (`wheel.py` conviction_trim field, `regime_router.py` TRIM_FRACTION + fork + counters, `test_regime_router.py` trim tests). Check `git status`.

Remaining steps:
1. [ ] Commit the engine+tests if uncommitted: `git add -A && git commit` (msg: "feat: conviction trim — half-size trend holds in stressed vol (default off) + edge tests"). Full suite must be green first: `PYTHONPATH=. .venv/bin/python -m pytest tests/engine_v2 -q`.
2. [ ] Referee (plan Task 3): extend `audit_defense_execution.py` `audit_router()` — when `conviction_trim=True`, every `BUY_SHARES` on a derived stressed-uptrend day must be `full//2`, else full. Reconstruct full lots from the ledger alone: `before = tr.cash_after + tr.contracts*100*tr.price_per_contract; full = int(before // (tr.price_per_contract*100))`. Double-entry. Run `--router` (both trim on+off arms) → exit 0 on all 4 tickers.
3. [ ] A/B runner (plan Task 4): add a `ROUTER+trim` arm (third line) to `scripts/run_regime_router.py` per ticker, keeping both benchmarks; headline metric = **max drawdown**; print `trimmed N / half-days M`. Run it → `data/options/reports/regime_router.txt`.
4. [ ] code-review workflow (high) on the branch diff; fix verified findings; re-run suite + `--router`.
5. [ ] Merge `conviction-trim` → main (`--no-ff`, default-off), delete branch, tag optional.
6. [ ] Update vault `09 Chameleon/_STATUS.md` (v1.1 + A/B verdict) + `Logs/2026-07-15.md` + memory `chameleon-bot.md`. ELI10 the A/B result into the morning report.

Recorded ex-ante expectation (for judging): trim likely lowers total return slightly, lowers max drawdown. Success = smoother ride for small give-up. Kill = lower return AND no drawdown help. Owner judges.

## JOB 2 — deep fresh-eyes audit of the entire Chameleon bot (READ-ONLY)

Goal: prove it works to a tee, or find where it doesn't. Fresh context — read each module cold, re-derive correctness, do not assume our chat's claims.

Scope (all of it):
- **Weather/regime** `src/engine_v2/regime/state.py` (trend from 200d/50d MA, vol percentile; no-look-ahead), `base_rates.py`, `autopsy.py`, `data.py` (closes source).
- **Wheel engine** `src/engine_v2/options/wheel.py` (entry/TP/roll/stop/expiry/basis floor, gates, all cash accounting).
- **Chameleon router** `src/engine_v2/options/regime_router.py` (posture machine, borders, conviction trim, equity marking).
- **Referees** `scripts/audit_defense_execution.py` (all modes: default, --hourly, --portfolio, --router) — and confirm the referees themselves are honest (independent re-derivation, not importing engine decision code).

How to audit (suggested):
1. Empirical stress — run EVERY referee to exit 0: `PYTHONPATH=. .venv/bin/python -m scripts.audit_defense_execution` (+ `--hourly`, `--portfolio`, `--router`). Run full suite + coverage `--cov=src/engine_v2` (≥95%). Record numbers.
2. Fresh-eyes code audit — dispatch parallel subagents (Agent tool), one per module above, each reading cold with a charter: find look-ahead, cash-accounting errors, off-by-one in state lookups (strictly-prior + 14d staleness), posture-machine holes, referee circularity, and any place the byte-identical/invariant claims could break on data the anchor test doesn't exercise (data gaps, missing marks, single-lot, split-broken windows).
3. Adversarial property checks — e.g. no-look-ahead property (future state rows never change decisions) across all four tickers, not just synthetic; plain-path digest still byte-identical; regime state matches known dates (2020-03 SPY downtrend+stressed).
4. Compile ONE ranked findings report (most-severe first) → write to `docs/superpowers/AUDIT-REPORT-2026-07-15.md` AND summarize (ELI10) in chat for the morning. If zero real defects, say so plainly with the evidence.

## Progress log (append as you go)
- 2026-07-15: handoff written. Conviction-trim engine+tests done, commit pending.
- 2026-07-15: conviction-trim engine+tests COMMITTED (63db11a). Referee `--router` extended + verifying half-sizing, 0 mismatches (committed). A/B runner arm added + run (committed). Full suite 330 green. A/B VERDICT (mixed): SPY +148.6%→+109.0% maxDD −31.0%→−20.3% Sharpe 0.61→0.66 (WIN); GDX +239→+217 maxDD −48.5→−37.7 Sh 0.55→0.58 (WIN); SLV +265→+139 maxDD −50→−38.6 Sh 0.60→0.49 (LOSS, halved return, Sharpe down); XOP +161→+109 maxDD −44.3→−44.6 Sh 0.63→0.56 (KILL cell: lower return, no dd help). Verdict: helps SPY/GDX drawdown, hurts SLV/XOP. Underperformed even its modest ex-ante expectation. Recommend MERGE default-off (dormant, referee-verified) but keep OFF pending fresh-data validation.
- 2026-07-15: code-review DONE (4 findings fixed: live TRIM_FRACTION, robust audit reconstruction + cfg.contract_multiplier, solo-wheel conviction_trim guard). MERGED conviction-trim→main default-off @ 96add22, tag `router-v1.1-trim`, branch deleted. 332 green, --router 0 mismatches. Vault `09 Chameleon/_STATUS.md` v1.1 + `Logs/2026-07-15.md` updated. **JOB 1 COMPLETE.**
- NEXT: **JOB 2 — deep read-only Chameleon audit.** Launch fresh-eyes parallel Agent auditors (one per module: regime/state.py+base_rates+autopsy, options/wheel.py, options/regime_router.py, scripts/audit_defense_execution.py referees) + run ALL referees to exit 0 + full suite/coverage + no-look-ahead property on real tickers + known-date regime checks. Compile ranked findings → `docs/superpowers/AUDIT-REPORT-2026-07-15.md` + ELI10 chat summary. READ-ONLY: report, do not patch merged code.

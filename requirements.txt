pandas>=2.0
numpy>=1.24
scipy>=1.10
yfinance>=0.2.40
pyarrow>=14.0
pytest>=8.0
pytest-cov>=4.0
streamlit>=1.37
plotly>=5.20
scikit-learn>=1.3
# live/ only: live/data.py imports schwab.client inside its chain-pull functions.
# Was absent while pytest.ini's testpaths excluded live/, so CI never needed it.
schwab-py>=1.5
